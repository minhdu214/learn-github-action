name: Duc-test-github-action
on:
  push:
    path: 
    - '!README.md'
    - '!helo/**'
    - '**.js'
    branches:
    - 'main'
    - 'release/**'
    - '!dev/**'
    workflow_dispatch:
jobs: 
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo Hello world
